# Metrics

## Recall at K

The primary documented metric is recall at K. Parser defaults and local evaluators include `R@1`, `R@5`, `R@10`, and `R@100`.

## Attack runtime and query counts

`perceptual_eval.py` records dataset results, runtimes, and query counts for attack evaluation.

## Notes

No separate metric configuration file was found in the root repository.
